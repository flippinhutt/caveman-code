Build site: context/plans/build-site-token-efficiency.md
# T-te-130: Reflexion-lite single retry with depth ≤2 and same role tag
**Status:** done
`canRetryReflexion(state)` returns `state.depth < 2`. `incrementReflexion(state)` throws past max depth. State carries `roleTag` so retries stay in the same role.
Tests: allow at 0/1, reject at 2, throw past max, carry role tag.
Acceptance R6 AC-1, AC-2, AC-3 met.
