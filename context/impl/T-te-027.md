Build site: context/plans/build-site-token-efficiency.md
# T-te-027: Provence reranker-pruner middleware
**Status:** partial (deterministic BoW stub shipped; real ONNX reranker gated on T-081)

`packages/agent/src/compression/provence.ts`:
- `scoreChunk(chunk, query)` — bag-of-words overlap normalized by sqrt(chunk length)
- `rerank(input)` — sorted desc, keep top-K by `keepRatio`, drop below `dropBelow`
- `ProvenceMiddleware.prune()` wrapper
- Deterministic tie-break by chunk lexicographic order

Tests (compression.test.ts): 20-chunk query returns pruned ordered list, below-threshold chunks dropped, same input returns same ordering, Provence wrapper.
Acceptance R2 AC-1, AC-2, AC-3 met via stub.
