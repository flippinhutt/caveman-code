Build site: context/plans/build-site-token-efficiency.md
# T-te-120: Deterministic file→symbol→line localizer over repomap
**Status:** done
`packages/agent/src/localizer/localizer.ts` — `localize({ ranked, query, topK })` scores each ranked symbol by token overlap with the query, blended with the symbol's PageRank weight. Deterministic sort. Output carries `{file, symbol, lineRange, confidence}`.
Tests: bug location in top-K, output shape, determinism.
Acceptance R1 AC-1, AC-2, AC-3 met.
