Build site: context/plans/build-site-token-efficiency.md
# T-te-028: Tool result cache keyed by (tool, args, fingerprint)
**Status:** done

`packages/agent/src/tools/result-cache.ts`:
- `canonicalJson()` sorts object keys so arg ordering is semantic-equivalent
- `keyHash({sessionId, tool, args, fingerprint})` → SHA256 of canonical key
- `ToolResultCache.put/get` store and retrieve; `hits` counter per entry
- Fingerprint: `{ gitSha?, mtime?, size? }` tuple supplied by caller

Tests (result-cache.test.ts): two identical reads produce one write / one hit, semantic-equivalent arg ordering hits same entry, fingerprint change causes miss, session isolation, keyHash determinism, bypass semantics.
Acceptance R1 AC-1..AC-4 met.
