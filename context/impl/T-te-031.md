Build site: context/plans/build-site-token-efficiency.md
# T-te-031: Normalization byte-identical across wall-clock runs
**Status:** done

Test: "same-file reads at different times are byte-identical after normalize" — two inputs that differ only in ISO timestamps are normalized to the same string.

Acceptance R2 AC-1 met.
