Build site: context/plans/build-site-token-efficiency.md
# T-te-126: Best-of-N parallel subagent sampling (N default 1)
**Status:** done
`verifyBestOfN(candidates, { n, ... })` runs `runTest` against the top-N candidates. N defaults to 1 via the caller's config.
Tests: "N=3 produces three attempts", "default N=1 only tests the first".
Acceptance R3 AC-1, AC-2 met. Parallel execution is the caller's responsibility (spawn subagent workers).
