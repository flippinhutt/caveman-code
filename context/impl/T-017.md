# T-017: RTK Binary Detection Module
**Status:** done

Implemented.

Files changed:
- `packages/coding-agent/src/core/rtk.ts`
- `packages/coding-agent/test/rtk.test.ts`

Validation:
- `cd packages/coding-agent && ../../node_modules/.bin/tsx ../../node_modules/vitest/dist/cli.js --run test/rtk.test.ts`
- `npm run check`

Acceptance criteria met:
- AC-1: met — `detectRtk()` resolves `{ available: true, version: stdout.trim() }` when `rtk --version` exits `0`
- AC-2: met — spawn/path failures resolve `{ available: false, version: null }` without throwing
- AC-3: met — non-zero exit from `rtk --version` resolves unavailable
- AC-4: met — `getRtkStatus()` caches first detection result and reuses it until `resetRtkCache()`
- AC-5: met — version string stored alongside availability result

Note:
- `packages/coding-agent/src/core/rtk.ts` already existed in repo; task implemented in existing module rather than creating brand-new file.
