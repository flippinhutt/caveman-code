Build site: context/plans/build-site-token-efficiency.md
# T-te-032: Tree-sitter parser layer for TS/JS/Py/Go/Rust/Java/C/C++
**Status:** partial (interface + regex heuristic impl; real tree-sitter bindings deferred)

`packages/agent/src/repomap/parser.ts`:
- `RepoLanguage` type covers 8 supported languages
- `languageFor(filename)` classifies by extension
- `parseFile(file, source)` → `ParsedFile` with `ParsedSymbol[]`
- Per-language regex heuristic extracts function/class/type/const with file+line
- `isSupported()`, `SUPPORTED_LANGUAGES` exposed

Tests (repomap.test.ts): TS function/class/type/const extraction, Python def/class, Go func/type/const, Rust fn/struct/trait/const, symbol file+line, deterministic parse.

Real tree-sitter bindings land in a follow-up that drops `tree-sitter` deps and reuses the `ParsedFile` interface — no downstream refactor required.
Acceptance R1 AC-1, AC-3 met at contract level.
