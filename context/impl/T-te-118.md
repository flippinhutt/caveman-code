Build site: context/plans/build-site-token-efficiency.md
# T-te-118: Hunk-level inline review UI consuming edit-tools diff payload
**Status:** done (data layer); TUI presentation wiring follow-up
`packages/agent/src/review/hunk-review.ts` — `applyReview(originalContent, payload, decisions)` takes a `DiffPayload` (T-101) and per-hunk `HunkDecision[]`, and applies accepted hunks in reverse-line order so coordinates don't shift. Rejected hunks leave their region byte-identical.
Tests: auto-accept applies all, rejected keeps byte-identical, per-hunk review, decision-count mismatch error.
Acceptance R6 AC-1, AC-4 met.
