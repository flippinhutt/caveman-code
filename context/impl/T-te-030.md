Build site: context/plans/build-site-token-efficiency.md
# T-te-030: Tool output normalization
**Status:** done

`normalizeToolOutput(output, workdir)`:
- Strips ANSI escape sequences
- Rewrites absolute `workdir` path → `.`
- Redacts ISO-8601 timestamps → `<ts>`
- Collapses CRLF → LF
- Strips trailing whitespace per line

Tests: ANSI strip, path rewrite, timestamp redaction, CRLF/whitespace.
Acceptance R2 AC-2, AC-3, AC-4 met.
