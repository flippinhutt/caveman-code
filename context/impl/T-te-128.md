Build site: context/plans/build-site-token-efficiency.md
# T-te-128: Tie-break equal-passing candidates by smallest-diff deterministic key
**Status:** done
`passed.sort((a, b) => a.diffSize - b.diffSize || a.id.localeCompare(b.id))` — smallest diff first, ID lex on tie.
Test: "tie-break by smallest-diff deterministic key".
Acceptance R4 AC-2 met.
