Build site: context/plans/build-site-token-efficiency.md
# T-te-026: LLMLingua-2 ONNX middleware
**Status:** partial (interface + deterministic JS stub shipped; real ONNX wire gated on T-081)

`packages/agent/src/compression/llmlingua.ts` — `LLMLinguaMiddleware` implements `CompressionMiddleware`.

- Halves a ~4000-token block at `targetRatio=0.5` within ±10% tolerance
- Runs without spawning Python process (pure JS)
- Ratio configurable via `CompressionOptions.targetRatio`
- Passthrough under activation threshold
- Deterministic: 100-run byte stability test
- ONNX runtime path present but intentionally throws until T-081 lands

Tests: compression.test.ts (5 llmlingua + 1 deterministicCompress).
Acceptance R1 AC-1, AC-2, AC-3 met via stub. R4 (determinism) met.
