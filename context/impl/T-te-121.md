Build site: context/plans/build-site-token-efficiency.md
# T-te-121: Feed localizer into agent loop first turn (replace/augment toggle)
**Status:** done
`localizerFeed(candidates, { mode })`:
- `replace`: returns a block replacing the default first-turn context
- `augment`: returns a block appended alongside
- `off`: returns empty string
Tests: replace mode builds block, off mode empty.
Acceptance R2 AC-1, AC-2, AC-3 met.
