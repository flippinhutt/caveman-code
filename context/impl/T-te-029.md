Build site: context/plans/build-site-token-efficiency.md
# T-te-029: Session scope + two-session isolation test
**Status:** done

`sessionId` is part of the cache key, so entries from `s1` never collide with entries from `s2` even with identical `(tool, args, fingerprint)`.

Test: "two sessions do not share entries" puts under `s1`, asserts `s2` miss.

Acceptance R1 AC-4 met.
