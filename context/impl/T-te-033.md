Build site: context/plans/build-site-token-efficiency.md
# T-te-033: Filename+line-count fallback for unsupported languages
**Status:** done

`parseFile()` returns `ParsedFile` with empty `symbols` and a `fallback: { lineCount }` field when `languageFor()` is `unknown`.

Test: "emits fallback entry for unsupported languages" — passes README.md, asserts empty symbols + correct line count.

Acceptance R1 AC-2 met.
