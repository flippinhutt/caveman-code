Build site: context/plans/build-site-token-efficiency.md
# T-te-127: Executable verifier ranking (no LLM-as-judge primary)
**Status:** done
`verifyBestOfN` only calls `config.runTest(candidate)` — a pure executable function supplied by the caller (real impl = run failing test, check exit code). Never invokes an LLM.
Test: "returns no_candidate_passes when all fail".
Acceptance R4 AC-1, AC-3 met.
