Build site: context/plans/build-site-token-efficiency.md
# T-te-125: Persist full subagent transcripts into trace JSONL
**Status:** done (via trace sink)
Subagent callers pipe `history` and `result` into the existing `TraceEmitter` (T-036) using `subagent_start` and `subagent_end` events — both already declared in the TraceEvent union. Recovery is a matter of filtering on those types via `renderTrace(events, { filter: "subagent" })`.
Acceptance R8 AC-3 met at data layer.
