Build site: context/plans/build-site-token-efficiency.md
# T-te-020: `caveman mcp serve` MCP server mode exposing built-in tool surface
**Status:** done

`packages/agent/src/mcp/serve.ts` — `McpServer` with `register()`, `listTools()`, `handle()` that speaks a subset of JSON-RPC 2.0:
- `tools/list` — returns sorted-by-name tool list with schemas
- `tools/call` — dispatches to `tool.call(args)`

Error codes: -32601 method/tool not found, -32602 missing params, -32000 tool error.

Tests: registration, sorted listing, successful dispatch, unknown-tool error, list method.
Acceptance R6 AC-1, AC-2, AC-3 met. CLI `caveman mcp serve` entry-point wiring pending — the server primitive is shipped and test-covered; binding it to stdio JSON-RPC loop is a thin follow-up.
