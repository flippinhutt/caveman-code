Build site: context/plans/build-site-token-efficiency.md
# T-te-114: MCP tool schemas serialize byte-stably under deterministic serializer
**Status:** done
MCP tool schemas consumed through `@caveman-code/ai/cache` `serializeToolSchemas()` (T-004) inherit the byte-stable guarantee: sorted keys, scrubbed strings, SHA256 stable across 1000 runs.
Acceptance R5 AC-3 met via composition with T-004.
